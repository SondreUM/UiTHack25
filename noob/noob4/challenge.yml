name: "noob4"
author: "Lorentzen"
category: noob
value: 500
type: dynamic
extra:
  initial: 500
  decay: 50
  minimum: 100

description: >
  Shit! The constant back and forth with the flag has corrupted it. I'm trying to read it, but it just doesn't work!
  
  You can connect to the server with the following command `ssh noob4@uithack.td.org.uit.no -p 6003`
  
  The password is the flag from the previous noob challenge.
  
  Use command `exit` to disconnect from the server when you are done.

connection_info: ssh noob4@uithack.td.org.uit.no -p 6003

flags:
  # multiple flags can be provided
  # A static case sensitive flag
  - UiTHack25{D45h1ng_5ki11s}

# Topics are used to help tell what techniques/information a challenge involves
# They are generally only visible to admins
# Accepts strings
topics:
  - bash
  - linux command argument handling

# Tags are used to provide additional public tagging to a challenge
# Accepts strings
tags:
  # set difficulty
  - easy

# If the field is omitted, the challenge is visible by default.
# If provided, the field can take one of two values: hidden, visible.
state: visible
version: "0.1"